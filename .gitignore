# Secrets
.env

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# AI / IDE tools
.claude/

# Runtime data (all stored inside project dir)
temp_audio/
screenshots/
logs/
usage.json

# Piper model weights (large binary — download separately)
models/*.onnx
models/*.onnx.json

# Project notes (local only)
What have we done till now.txt

# Android build artifacts
android-app/.gradle/
android-app/build/
android-app/app/build/
android-app/local.properties
android-app/.idea/
android-app/**/*.iml

dist/
*.egg-info/