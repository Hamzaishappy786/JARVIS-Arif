# Secrets
.env

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Temp audio
temp_audio/*.wav

# Logs (keep folder, ignore contents)
logs/*.log
logs/*.txt
logs/*.json

# Piper model weights (large binary — download separately)
models/*.onnx
models/*.onnx.json

# Android build artifacts
android-app/.gradle/
android-app/build/
android-app/app/build/
android-app/local.properties
android-app/.idea/
android-app/**/*.iml
